<Page x:Class="FoodBankInventoryManager.BarcodeCreatorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FoodBankInventoryManager"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="725"
      Title="Barcode Creator">
    <Page.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="True" 
                                   Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" 
                                   StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="7" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtBarcodedata" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Height="22" 
                 Margin="100,0,100,0" VerticalAlignment="Bottom" MaxLines="1" MinLines="1" KeyDown="txtBarcodedata_KeyDown"/>

        <Button x:Name="btnGenerateBarcode" Click="btnGenerateBarcode_Click" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" 
                Height="100" Width="150" Grid.ColumnSpan="2" Margin="0,20,174,100" Grid.RowSpan="2">
            <TextBlock TextWrapping="Wrap" FontSize="24">Generate Barcode</TextBlock>
        </Button>

        <StackPanel Orientation="Vertical" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Left" Height="383" Margin="91,34,0,0" Grid.Row="1" 
                    Grid.RowSpan="4" VerticalAlignment="Top" Width="366" Grid.Column="1">
            <TextBlock FontSize="18" HorizontalAlignment="Center" >*Barcode Image Preview*</TextBlock>
            <Border  BorderBrush="Black"  BorderThickness="5" Height="244">
                <Image x:Name="imgBarcode"  />
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                <Button x:Name="btnAddtoPrint" FontSize="22" Width="173" Height="64" Click="btnAddtoPrint_Click">
                    <TextBlock TextWrapping="Wrap" TextAlignment="Center">Add to Print Preview</TextBlock>
                </Button>

                <Button x:Name="btnPrintBarcode" Content="Print Barcode(s)" FontSize="22"  Width="173" Height="64" Click="btnPrintBarcode_Click">
                </Button>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Margin="72,72,214,10" Grid.Row="2" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontSize="16" Margin="0,0,0,0">Number of Barcodes in Preview:</TextBlock>
            <TextBlock x:Name="txtNumBarcodes"  FontSize="24" TextAlignment="Center">0</TextBlock>
            <Button Width="150" Click="Button_Click">View current values</Button>
        </StackPanel>

        <Button x:Name="bttnHome" HorizontalAlignment="Left" VerticalAlignment="Top" Height="40" Width="40" FontSize="22" FontWeight="Bold" Click="bttnHome_Click">⌂</Button>

    </Grid>
</Page>
